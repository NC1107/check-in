include: package:flutter_lints/flutter.yaml

formatter:
  page_width: 100

linter:
  rules:
    prefer_const_constructors: true
    avoid_print: true
analyzer:
  exclude:
    - build/**
    - android/**
    - ios/**
    - web/**
    - windows/**
    - macos/**
    - linux/**
